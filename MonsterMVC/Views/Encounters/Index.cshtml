@model IEnumerable<MonsterMVC.Domain.Data.Encounter>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using (Html.BeginForm("Create", "Encounters", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

    <a href="Encounters/CreateFromRandom">  <input type="submit" value="Create" class="btn btn-default" href="CreateFromRandom.cshtml" /></a>
              
        